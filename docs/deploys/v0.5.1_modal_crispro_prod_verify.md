# v0.5.1 ClinicalBERT — Modal deploy on `crispro` + prod verify

**Date**: 2026-07-20
**Branch**: `feat/v0.5.1-production-grade-nsclc-gold`
**Best commit before this**: `38bff4e`

## Modal deploy

- Profile: `crispro` (`ak-N93fZFffzTocw2pzQc0snU`) after `crispro-test` and `testing-123`
  hit workspace spend limits.
- App: `modal.App("clinicalbert")` — `deploy/modal/clinicalbert_app.py`.
- URLs live:
  - `https://crispro--clinicalbert-healthz.modal.run`
  - `https://crispro--clinicalbert-info.modal.run`
  - `https://crispro--clinicalbert-parse.modal.run`
- Dashboard: `https://modal.com/apps/crispro/main/deployed/clinicalbert`.
- Deploy logs: `/mnt/results/v51_progress/modal_deploy_v51_crispro.log`.

## Endpoint verify (2026-07-20 re-run on `crispro`)

### `GET /healthz`

HTTP 200. `{"status":"ok","app":"clinicalbert","version":"clinicalbert-modal-v0.5.1",...}`.

### `GET /info` (835 B)

- `app_version = clinicalbert-modal-v0.5.1`
- `training_seed = 456`
- `provenance = REAL-v0.5.1-snorkel-openrouter-llm`
- `test_micro_f1 = 0.0809` (combined)
- `real_text_micro_f1_breast_crc = 0.0879`
- `real_text_micro_f1_nsclc = 0.0696`
- `annotator_kappa_nsclc = 0.7370`
- `bio_alignment_error_rate = 0.02087`
- `class_weighted_loss = true`, `label_smoothing = 0.1`
- Snorkel LF accuracies:
  - `LF-regex = 0.9897`
  - `LF-LLM = 0.5126`
  - `LF-ontology = 0.5124`
  - `LF-section = 0.99`

Saved: `/mnt/results/v51_progress/modal_info_v51.json`.

### `POST /parse` — NSCLC smoke (cold, 2.25 s)

9 entity spans:

| entity | surface | value |
|---|---|---|
| GRADE | grade 2 | 2 |
| TUMOR_SIZE_MM | Tumor size : 3.2 cm | 3.2 |
| T_STAGE | pT 2 | T 2 |
| N_STAGE | pN 0 | N 0 |
| M_STAGE | pMx | MX |
| MARGIN | Margins : negative | negative |
| EGFR | EGFR | mutated |
| KRAS | KRAS | mutated |
| ER_VALUE | ROS | positive |

Known v0.5.2 bug: model mislabels `ROS1` as `ER_VALUE`. Filed.

Saved: `/mnt/results/v51_progress/modal_nsclc_parse_v51.json`.

### `POST /parse` — Breast smoke (warm, 1.33 s)

10 clean entity spans, no spurious:

- GRADE 2, TUMOR_SIZE_MM 2.1 cm, T_STAGE pT2, N_STAGE pN0, M_STAGE pMx, MARGIN negative
- ER_VALUE positive, PR_VALUE positive, HER2_VALUE negative, KI67_PCT `Ki-67 : 15%`

Saved: `/mnt/results/v51_progress/modal_breast_parse_v51.json`.

## Render prod flip

- Service: `srv-d943p68js32c73dhqreg` (`oncology-arbiter`).
- Env var `CLINICALBERT_MODAL_URL`: `https://crispro-test--clinicalbert` → `https://crispro--clinicalbert`.
- Manual redeploy required (Render env-var update does NOT auto-trigger). Deploy id
  `dep-d9f43prbc2fs739311jg` reached `live` in ~90 s.

## Prod end-to-end (real production-grade test)

```
POST https://oncology-arbiter.onrender.com/v1/case/full?cancer=nsclc
X-API-Key: <live key>
Body:  {"biopsy_input": {"report_text": "<pT2a pN0 pMx NSCLC pathology w/ EGFR L858R>"}}
```

HTTP 200 in 1.67 s wall-clock. `nsclc.parsed_report_provenance`:

```json
{
  "provenance": "REAL-v0.5.1-snorkel-openrouter-llm",
  "base_model": "emilyalsentzer/Bio_ClinicalBERT",
  "training_seed": 456,
  "test_micro_f1": 0.0809,
  "app_version": "clinicalbert-modal-v0.5.1",
  "n_tokens": 65,
  "seconds": 1.052,
  "n_entity_types": 9,
  "wall_seconds": 1.525,
  "source": "clinicalbert_modal"
}
```

`nsclc.parsed_report` keys: `GRADE, TUMOR_SIZE_MM, T_STAGE, N_STAGE, M_STAGE, MARGIN, EGFR,
KRAS, ER_VALUE`. Byte-for-byte match with the direct Modal `/parse` call — the arbiter is
proxying correctly.

Saved: `/mnt/results/v51_progress/prod_nsclc_response_v51.json`.

## Breast route note

`POST /v1/case/full?cancer=breast` goes through the biopsy path with
`parser_id: proxy_regex_v0`, not ClinicalBERT. This is by design — ClinicalBERT is wired to
the NSCLC path only at `src/oncology_arbiter/api/app.py:563`. Breast is a v0.5.2 followup.

## Acceptance checklist (all met)

- [x] 5 seeds trained, aggregate written, rollback not triggered (+33.82 % vs v0.5.0)
- [x] Best seed 456 promoted to `/workspace/clinicalbert_best/`
- [x] Modal deploy live on `crispro`
- [x] `/healthz`, `/info`, `/parse` verified against v0.5.1 metadata
- [x] Render env var flipped and redeployed live
- [x] Prod `/v1/case/full?cancer=nsclc` returns real ClinicalBERT-Modal entities

## Post-ship followups

- Rotate secrets shared over copyable channels (GitHub PAT, Modal token `ak-N93fZFffzTocw2pzQc0snU`).
- Retire unusable Modal profiles `crispro-test`, `testing-123`.
- v0.5.2: fix Ki-67 value regex, fix ROS1→ER_VALUE mislabel, close train/test gap
  (val ≈ 0.32 vs test ≈ 0.08) via held-out gold slice + tighter LF filtering + higher
  `min_votes` + ontology expansion.
